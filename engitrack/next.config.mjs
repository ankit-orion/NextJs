/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['media.istockphoto.com', 'www.google.com', 'images.unsplash.com'],
    },
  };
  
  export default nextConfig;